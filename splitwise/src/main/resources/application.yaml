spring:
  application:
    name: splitwise

  datasource:
    url: jdbc:mysql://localhost:3306/splitwise
    username: root
    password: Nidhi@2002

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format-sql: true

server:
  port: 9000
  servlet:
    context-path: /api